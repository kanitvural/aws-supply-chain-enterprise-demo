[
  {
    "name": "list_products",
    "description": "List all products in the inventory. Returns product IDs, names, quantities, and other details for every product.",
    "inputSchema": {
      "type": "object",
      "properties": {},
      "required": []
    }
  },
  {
    "name": "check_inventory",
    "description": "Check inventory details for a specific product. You MUST provide either product_id or product_name.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "product_id": {
          "type": "string",
          "description": "The unique identifier of the product (e.g., P-001)."
        },
        "product_name": {
          "type": "string",
          "description": "The name of the product to search for if the ID is unknown."
        }
      },
      "required": []
    }
  },
  {
    "name": "update_inventory",
    "description": "Update the inventory quantity for a specific product using set, add, or subtract operations.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "product_id": {
          "type": "string",
          "description": "The unique identifier of the product to update."
        },
        "quantity": {
          "type": "integer",
          "description": "The quantity value to set, add, or subtract."
        },
        "operation": {
          "type": "string",
          "description": "The inventory operation to perform: set, add, or subtract. Defaults to set if not specified."
        }
      },
      "required": ["product_id", "quantity"]
    }
  }
]
